import * as React from 'react'
import {LocalBoardPreview, LocalBoardPreviewProps} from './LocalBoardPreview'

export const LocalGamePreview = (props: LocalBoardPreviewProps) => {
    return (
        <LocalBoardPreview {...props}/>
    )
}